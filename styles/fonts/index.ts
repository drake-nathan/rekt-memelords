import { Sono, Cutive } from '@next/font/google';

export const sono = Sono({
  weight: ['200', '300', '400', '500', '600', '700', '800'],
  style: ['normal'],
  display: 'swap',
});

export const cutive = Cutive({
  weight: ['400'],
  style: ['normal'],
  display: 'swap',
});
